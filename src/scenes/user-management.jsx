import { UserManagementContainer } from '~/modules/user/user-management'

export default class UserManagementScene {
    render() {
        return (
            <UserManagementContainer />
        )
    }
}